package Unit2.Lesson2_2

/*
- Create a class called `MyClass` that contains a `String` property called `myString`.
- Create a constructor for `MyClass` that takes a `String` as an argument and assigns it to `myString`.
- Create a `main` function that creates an instance of `MyClass` and prints out the value of `myString`.
 */